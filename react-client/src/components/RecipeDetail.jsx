// src/components/RecipeDetail.jsx
// Not used in the current version of the project.
// Kept as a placeholder so the file exists but doesn't pull in react-router.

export default function RecipeDetails() {
    return null;
  }